#include <iostream>
#include <bits/stdc++.h>
using namespace std;
#define int long long

signed main(){
	vector<int>v(10,100);
	v.push_back(69);
	for (int i = 0; i < v.size(); ++i)
	{
		cout<<v[i]<<endl;
	}
}